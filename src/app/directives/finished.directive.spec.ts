import { FinishedDirective } from './finished.directive';

describe('FinishedDirective', () => {
  it('should create an instance', () => {
    const directive = new FinishedDirective();
    expect(directive).toBeTruthy();
  });
});
